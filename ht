openDetailsModal(item) {
    this.selectedItem = item;
    console.log('Selected Item:', JSON.parse(JSON.stringify(this.selectedItem)));  // convert proxy to normal object
    $('#large').modal('show');
},
